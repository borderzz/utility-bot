/*module.exports = {
    name: 'die',
    description: 'kill',
    execute(message, args, Discord) {
        const borgers = '527893930805559330'
            if (message.author.id == borgers) {
            message.channel.send('okay master').then(() => {
                client.destroy();
               })
            }
            else {
                message.channel.send('good try loaser');
        }
    }
}
*/